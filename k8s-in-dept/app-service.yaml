apiVersion: v1
kind: Service
metadata:
 name: my-app-service
spec:
 type: ClusterIp
 selector:
  app: my-app #by this name lables in deployment
 ports:
  - protocol: TCP
    port : 80
    targetPort: 9376 #Service targets TCP port 9376 on any Pod with the app.kubernetes.io/name: MyApp label